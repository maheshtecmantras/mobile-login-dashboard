import { useNavigate } from 'react-router-dom';
import StatCard from '../components/StatCard.jsx';
const stats = [{label:'Tasks completed',value:'24',icon:'✓',tone:'success'},{label:'Active projects',value:'08',icon:'▦',tone:'primary'},{label:'Hours focused',value:'18.5',icon:'◷',tone:'warning'},{label:'Team updates',value:'12',icon:'✉',tone:'primary'}];
const actions = [{label:'New task',icon:'＋'},{label:'Projects',icon:'□'},{label:'Activity',icon:'◉'}];
export default function DashboardPage(){const navigate=useNavigate();const logout=()=>{sessionStorage.removeItem('loggedIn');navigate('/login');};return <main className="dashboard"><header><div><p>Good morning,</p><h1>Alex Morgan</h1></div><div className="avatar">AM</div></header><section className="banner"><div><strong>Your progress looks great!</strong><span>Keep your momentum going today.</span></div><b>✦</b></section><div className="section-heading"><h2>Overview</h2><button type="button">This week⌄</button></div><section className="stats">{stats.map((stat)=><StatCard key={stat.label} card={stat}/>)}</section><h2>Quick actions</h2><section className="actions">{actions.map((action)=><button type="button" key={action.label}><b>{action.icon}</b><span>{action.label}</span></button>)}</section><button className="logout" type="button" onClick={logout}>Log out</button></main>;}
